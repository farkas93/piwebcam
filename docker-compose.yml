version: '3.8'
services: 
  mjpeg-cam:
      container_name: mjpeg-cam
      build: .
      #volumes:
        #- /run/udev:/run/udev:ro
        #- /opt/vc:/opt/vc
      environment:
        - LOG_LEVEL=INFO
        - LD_LIBRARY_PATH=/opt/vc/lib
      restart: unless-stopped
      ports:
        - "8000:8000"
